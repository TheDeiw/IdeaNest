# 🔥 ДЕТАЛЬНА ІНСТРУКЦІЯ: Налаштування Firebase Storage

## 📋 Що таке Firebase Storage?

Firebase Storage - це хмарне сховище для зберігання файлів користувачів (фото, відео, документи). У нашому додатку ми використовуємо його для зберігання фотографій профілю.

---

## 🎯 Крок 1: Увійдіть в Firebase Console

### 1.1 Відкрийте браузер
Відкрийте Chrome, Firefox або інший браузер.

### 1.2 Перейдіть за посиланням
```
https://console.firebase.google.com
```

### 1.3 Виберіть свій проект
1. На головній сторінці знайдіть проект **"ideanest-2026"**
2. Клікніть на нього

**Що побачите:** Відкриється сторінка проекту з лівим меню

---

## 🎯 Крок 2: Увімкніть Firebase Storage

### 2.1 Відкрийте розділ Storage

**У лівому меню знайдіть "Storage":**

```
┌─────────────────────────┐
│ ☰ Firebase Console      │
├─────────────────────────┤
│   Authentication        │
│   Firestore Database    │
│ ▶ Storage              │ ← КЛІКНІТЬ СЮДИ
│   Hosting              │
│   Functions            │
└─────────────────────────┘
```

### 2.2 Натисніть "Get Started"

Якщо це перший раз:
1. Побачите велику кнопку **"Get Started"**
2. Натисніть на неї

### 2.3 Виберіть режим безпеки

Відкриється діалогове вікно з 2 варіантами:

**Варіант 1:** Start in production mode ✅ (ОБЕРІТЬ ЦЕЙ)
```
⚫ Start in production mode
   Recommended. Requires authentication and 
   explicit security rules.
```

**Варіант 2:** Start in test mode ❌ (НЕ ОБИРАЙТЕ)
```
○ Start in test mode
   Not recommended. Anyone can read/write your data 
   for 30 days.
```

**Натисніть "Next" →**

### 2.4 Виберіть локацію (регіон)

Відкриється вікно вибору локації:

**Рекомендовані регіони для України:**
- `europe-west1` (Бельгія) ✅ - найближчий до України
- `europe-central2` (Польща) ✅ - теж непоганий варіант
- `us-central1` (США) - якщо немає європейських

**Виберіть регіон та натисніть "Done"**

### 2.5 Зачекайте на ініціалізацію

Побачите екран завантаження:
```
Setting up Cloud Storage...
⏳ Creating storage bucket...
```

**Через 10-30 секунд** побачите:
```
✓ Cloud Storage is ready to use
```

---

## 🎯 Крок 3: Налаштуйте Security Rules

### 3.1 Відкрийте вкладку Rules

Зверху на сторінці Storage побачите 3 вкладки:
```
Files  |  Rules  |  Usage
       ↑
    КЛІКНІТЬ СЮДИ
```

### 3.2 Побачите редактор правил

Зараз там будуть дефолтні правила:
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

### 3.3 ВИДАЛІТЬ ВСЕ та вставте наші правила

**ВИДАЛІТЬ весь текст** і вставте це:

```javascript
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Правила для фотографій профілю
    match /profile_photos/{userId} {
      // Всі можуть читати фото профілю
      allow read: if true;
      
      // Тільки власник може завантажувати своє фото
      // Також перевіряємо що це зображення і розмір < 5MB
      allow write: if request.auth != null 
                   && request.auth.uid == userId
                   && request.resource.contentType.matches('image/.*')
                   && request.resource.size < 5 * 1024 * 1024;
      
      // Тільки власник може видаляти своє фото
      allow delete: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 3.4 Опублікуйте правила

**Знайдіть кнопку "Publish" (синього кольору) вгорі праворуч:**

```
┌────────────────────────────────┐
│ Rules          [Publish] ← ТУТ │
└────────────────────────────────┘
```

**Натисніть "Publish"**

**Побачите повідомлення:**
```
✓ Rules published successfully
```

---

## 🎯 Крок 4: Перевірте налаштування

### 4.1 Перевірте статус Storage

На вкладці "Files" має бути написано:
```
No files uploaded yet
```

Це нормально! Файли з'являться після першого завантаження фото.

### 4.2 Перевірте правила

На вкладці "Rules" має бути:
- ✅ Ваші правила (ті що ви вставили)
- ✅ Дата публікації: "Published ... ago"

---

## 🎯 Крок 5: Встановіть пакети у проекті

### 5.1 Відкрийте термінал

**Windows PowerShell або Command Prompt:**
```
Натисніть Win + R
Введіть: cmd
Натисніть Enter
```

### 5.2 Перейдіть в директорію проекту

```bash
cd "d:\University\5 Semestr\crossplatform\ideanest"
```

### 5.3 Встановіть пакети

```bash
flutter pub get
```

**Що побачите:**
```
Running "flutter pub get" in ideanest...
Resolving dependencies...
+ firebase_storage 12.3.0
+ image_picker 1.0.7
+ shared_preferences 2.2.2
Changed 15 dependencies!
```

**Зачекайте поки завершиться (10-30 секунд)**

---

## 🎯 Крок 6: Запустіть додаток

### 6.1 Переконайтеся що емулятор запущений

**Android Studio:**
- Device Manager → Запустіть емулятор
- Або підключіть реальний пристрій

### 6.2 Запустіть додаток

```bash
flutter run
```

**Що побачите:**
```
Launching lib\main.dart on sdk gphone64 x86 64...
Running Gradle task 'assembleDebug'...
✓ Built build\app\outputs\flutter-apk\app-debug.apk
Installing build\app\outputs\flutter-apk\app-debug.apk...
```

**Зачекайте 1-2 хвилини на першу компіляцію**

---

## 🧪 Крок 7: Протестуйте завантаження фото

### 7.1 Створіть новий акаунт або залогіньтесь

```
Email: test@test.com
Password: test123
Name: Test User
```

### 7.2 Відкрийте Settings

1. Відкрийте бічне меню (☰)
2. Натисніть "Settings"

### 7.3 Завантажте фото профілю

1. **Натисніть на аватар** (коло з іконкою людини)
2. Виберіть фото з галереї
3. Побачите індикатор завантаження
4. Через 2-5 секунд фото з'явиться

**Що має статися:**
```
Тап на аватар
    ↓
Відкривається галерея
    ↓
Вибираєте фото
    ↓
Показується ⏳ (loading)
    ↓
Фото завантажується в Firebase Storage
    ↓
URL зберігається в Firestore
    ↓
Фото відображається в аватарі ✓
```

---

## 🔍 Крок 8: Перевірте в Firebase Console

### 8.1 Перевірте Storage → Files

1. Відкрийте Firebase Console → Storage → Files
2. Має з'явитися папка **"profile_photos"**
3. Відкрийте її
4. Побачите файл: **"{userId}.jpg"**

**Приклад:**
```
profile_photos/
  └── Gvyg6EgmPGVF3xiR7R5hKZZbNpu1.jpg
```

### 8.2 Перевірте Firestore → Data

1. Відкрийте Firestore Database → Data
2. Відкрийте **users** → **{ваш userId}**
3. Має бути поле **photoURL** з посиланням

**Приклад:**
```
users/
  Gvyg6EgmPGVF3xiR7R5hKZZbNpu1/
    - displayName: "Test User"
    - email: "test@test.com"
    - photoURL: "https://firebasestorage.googleapis.com/..."
    - createdAt: December 5, 2024
    - updatedAt: December 5, 2024
```

---

## ❌ Можливі помилки та рішення

### Помилка 1: "Package not found"

```
Error: Target of URI doesn't exist: 'package:firebase_storage/firebase_storage.dart'
```

**Рішення:**
```bash
flutter clean
flutter pub get
```

---

### Помилка 2: "Permission denied" при завантаженні

```
[firebase_storage/unauthorized] User does not have permission
```

**Рішення:**
1. Перевірте чи правила опубліковані
2. Перевірте чи користувач залогінений
3. Перевірте правила ще раз

**Правильні правила:**
```javascript
allow write: if request.auth != null && request.auth.uid == userId;
```

---

### Помилка 3: "Storage bucket not found"

```
[firebase_storage/bucket-not-found]
```

**Рішення:**
1. Storage не увімкнений
2. Поверніться до Кроку 2
3. Увімкніть Storage

---

### Помилка 4: Фото не завантажується

**Перевірте:**
- [ ] Емулятор має доступ до інтернету?
- [ ] Firebase Storage увімкнений?
- [ ] Правила опубліковані?
- [ ] Користувач залогінений?

**Подивіться логи:**
```bash
flutter run --verbose
```

Шукайте рядки з `StorageService` або `firebase_storage`

---

## ✅ Контрольний список

Після налаштування перевірте:

- [ ] Firebase Storage увімкнений
- [ ] Регіон вибраний (europe-west1 або інший)
- [ ] Security Rules опубліковані
- [ ] `flutter pub get` виконаний успішно
- [ ] Додаток компілюється без помилок
- [ ] Можна завантажити фото профілю
- [ ] Фото відображається в Settings
- [ ] Фото зберігається в Firebase Storage
- [ ] photoURL зберігається в Firestore

---

## 📊 Що має бути в результаті

### Firebase Console → Storage → Files
```
📁 profile_photos/
   📄 {userId}.jpg (розмір: 50-200 KB)
```

### Firebase Console → Firestore → users/{userId}
```
displayName: "Your Name"
email: "your@email.com"
photoURL: "https://firebasestorage.googleapis.com/v0/b/ideanest-2026..."
createdAt: timestamp
updatedAt: timestamp
```

### Додаток → Settings
```
┌──────────────────────────┐
│  [ФОТО]   Your Account   │
│           Manage your... │
├──────────────────────────┤
│  👤 Full Name            │
│     Your Name        ✏️  │
│                          │
│  📧 Email Address        │
│     your@email.com       │
└──────────────────────────┘
```

---

## 🎯 Фінальна перевірка

### Тест 1: Завантаження фото
1. Settings → Тап на аватар
2. Вибрати фото
3. ✅ Фото з'явилося

### Тест 2: Фото зберігається
1. Вийти з додатку
2. Увійти знову
3. Settings
4. ✅ Фото досі там

### Тест 3: Firestore оновлено
1. Firebase Console → Firestore
2. users/{userId}
3. ✅ Є поле photoURL

### Тест 4: Storage має файл
1. Firebase Console → Storage
2. profile_photos/
3. ✅ Є файл {userId}.jpg

---

## 🎉 Готово!

Якщо всі тести пройшли успішно - Firebase Storage налаштований правильно!

Тепер можна:
- ✅ Завантажувати фото профілю
- ✅ Змінювати фото
- ✅ Видаляти фото
- ✅ Все зберігається в хмарі

**Налаштування завершене! 🚀**

---

## 📞 Якщо щось не працює

1. Перевірте всі кроки по черзі
2. Подивіться розділ "Можливі помилки"
3. Запустіть `flutter run --verbose` та подивіться логи
4. Перевірте правила в Firebase Console
5. Переконайтеся що користувач залогінений

**Успіхів з лабораторною! 🎓**

